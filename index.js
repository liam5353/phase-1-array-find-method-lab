// code your solution here
function superbowlWin(record) {
  let year = win.year;
  if (win.result === "W") {
    return win.year === "2015" } 
}

function superbowlWin(win) {
  let year = win.year;
  if (win.result === "W") {
    return win.year;    } 
    else {
    return undefined;
  }
}